services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: quantdata
      POSTGRES_USER: quantdata
      POSTGRES_PASSWORD: quantdata
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
